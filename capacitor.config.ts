import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'web-components-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
